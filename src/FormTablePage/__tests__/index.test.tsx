import React from 'react';
import { render } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import FormTablePage from '../index';

describe('FormTablePage', () => {
   it('renders without error', () => {

   });
});
