import React from 'react';
import { render } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import GenerateFormItem from '../index';

describe('GenerateFormItem', () => {
   it('renders without error', () => {

   });
});
