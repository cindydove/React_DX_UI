import React,{ memo } from 'react';
import { IProps } from './interface';

function {{pascalCase name}}(props:IProps){
    return <>{{pascalCase name}}</>
}

export default memo({{pascalCase name}});
